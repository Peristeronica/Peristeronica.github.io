# Assets

Static files used by the public site.

- `css/`: shared and page-specific stylesheets
- `js/`: page behavior
- `images/`: optimized images loaded by pages
- `source-images/`: original image sources used for regeneration
